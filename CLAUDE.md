# まとめときや × Claude Code

このリポジトリでClaude Codeを使う人向けの指示書です。まとめときやと連携（アカウント画面の「Claude連携」）を済ませたユーザーが、日本語で頼んだことをそのまま実行できるようにするための対応表とルールを書いています。

## 前提

- 連携が済んでいると、このディレクトリの`.env`に`MATOME_TODO_API_URL`・`MATOME_TODO_API_TOKEN`（読み取り用）・`MATOME_TODO_API_WRITE_TOKEN`（書き込み用）が入っています
- まだ連携していない場合は、アプリのアカウント画面で「連携する」を押して8桁コードを出してもらい、`python scripts/connect_matome.py <コード>`を実行します
- 各スクリプトは`.env`を自動で読み込みます

## 言われたこと → 実行すること

| ユーザーの言い方 | 実行するコマンド |
| --- | --- |
| 「Todoを見せて」「今日やること教えて」 | `python scripts/fetch_matome_todos.py` |
| 「このTodo終わった」「〇〇完了にして」 | `python scripts/complete_matome_todo.py <TodoのID>` |
| 「メモを見せて」「何を書いてるかまとめて」 | `python scripts/fetch_matome_notes.py` |
| 「〇〇というメモを作って」 | `python scripts/create_matome_note.py "<タイトル>" --content "<本文>" [--parent-id <親メモのID>]` |
| 「メモを直して」「本文を書き足して」 | `python scripts/update_matome_note.py <メモのID> [--title "..."] [--content "..."]` |
| 「〇〇にチェックつけて／外して」 | `python scripts/update_matome_note.py <メモのID> --check` / `--uncheck` |
| **「メモを消して」「〇〇いらない」** | **`python scripts/delete_matome_note.py <メモのID>`（＝ゴミ箱へ移動）** |

メモのIDは`fetch_matome_notes.py`の出力に含まれています。ユーザーはIDではなくメモの名前で言ってくるので、まず一覧を取得して対象を特定してください。

## メモを消すときのルール

「消して」と言われたら、**削除ではなくゴミ箱への移動**として扱ってください。`delete_matome_note.py`（`DELETE /api/v1/notes/<id>`）はアプリの削除ボタンと同じソフトデリートで、Firestoreのドキュメント自体は残り、アプリ画面のゴミ箱からいつでも復元できます。

守ること：

1. **実行前に対象を確認する。** 「〇〇というメモをゴミ箱に移動します」と伝えてから実行します。同じような名前のメモが複数ある場合は、どれか必ず聞き返してください
2. **子メモも一緒に移動することを伝える。** 子メモがあるメモを消すと、子孫メモもまとめてゴミ箱に入ります。件数を先に伝えてください
3. **完全削除はしない。** Claude Code側から完全削除する手段は用意していません。完全に消したい場合はアプリのゴミ箱から本人が操作します
4. **鍵付きメモは触らない。** 鍵付きメモは、ユーザーがアプリの「Claude連携」でトグルをONにしていない限り削除・更新できません（403が返ります）。403が返ったらそのまま伝え、勝手に回避策を探さないでください
5. **一度に大量に消さない。** 複数消す指示でも、対象の一覧を出して同意を取ってから実行します

## 変更はアプリ側に即反映される

まとめときやを開いたままでも、ここからの作成・更新・削除はリアルタイムで画面に反映されます。ユーザーに再読み込みを促す必要はありません。

## 鍵付きメモの扱い

鍵付きメモの本文は、ユーザーがアプリの「Claude連携」でトグルをONにしていない限り、読み取りAPIでも空で返ります。`🔒`が付いていて本文が無いメモは、内容を推測して埋めたりせず、鍵付きであることをそのまま伝えてください。
